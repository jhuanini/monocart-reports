# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests/ebr/core/payments/payment-order.spec.js >> business owner - bank account - points - credit - Mastercard - partial - now
- Location: utils/ebr/runtime-skip.js:399:27

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('.popup-layout').locator('..')
Timeout: 5000ms
Expected pattern: /Total\s*\$731\.42/
Received string:  "SummaryYou will earn 722 PointsPaying on behalf ofTest Business - ABN 15 636 708 471Paying toDon't Delete PayeeBSB: 063-238Account number: 123456789Reference: v666j0xxDescription: xyto1uc6Amount$721.79Feesprogram fee (1.045% inc. GST)$7.54Processing fee* (1.32% inc. GST)$9.63Total fees$17.17*Program fee is charged on the portion of the amount paid via the selected payment method.*Processing fee is charged on the proportion of amount plus program fee paid via the selected payment method.Total$738.96Paying fromPoints redeemed-16 PTS $0.12····
CARD HOLDER NAME UPDATED, *1005  (Points earned on this amount) $721.67····
CARD HOLDER NAME UPDATED, *1005 (Total fees)$17.17BackConfirm"

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('.popup-layout').locator('..')
    14 × locator resolved to <div class="relative flex h-full w-full flex-col max-h-screen-90">…</div>
       - unexpected value "SummaryYou will earn 722 PointsPaying on behalf ofTest Business - ABN 15 636 708 471Paying toDon't Delete PayeeBSB: 063-238Account number: 123456789Reference: v666j0xxDescription: xyto1uc6Amount$721.79Feesprogram fee (1.045% inc. GST)$7.54Processing fee* (1.32% inc. GST)$9.63Total fees$17.17*Program fee is charged on the portion of the amount paid via the selected payment method.*Processing fee is charged on the proportion of amount plus program fee paid via the selected payment method.Total$738.96Paying fromPoints redeemed-16 PTS $0.12




CARD HOLDER NAME UPDATED, *1005  (Points earned on this amount) $721.67




CARD HOLDER NAME UPDATED, *1005 (Total fees)$17.17BackConfirm"

```

```yaml
- banner:
  - heading "Summary" [level=2]
- text: "You will earn 722 Points Paying on behalf of Test Business - ABN 15 636 708 471 Paying to Don't Delete Payee BSB: 063-238 Account number: 123456789 Reference: v666j0xx Description: xyto1uc6 Amount $721.79 Fees program fee (1.045% inc. GST) $7.54 Processing fee* (1.32% inc. GST) $9.63 Total fees $17.17 *Program fee is charged on the portion of the amount paid via the selected payment method. *Processing fee is charged on the proportion of amount plus program fee paid via the selected payment method. Total $738.96 Paying from Points redeemed-16 PTS $0.12 CARD HOLDER NAME UPDATED, *1005 (Points earned on this amount) $721.67 CARD HOLDER NAME UPDATED, *1005 (Total fees) $17.17"
- contentinfo:
  - button "Back"
  - button "Confirm"
```

# Test source

```ts
  877  |     await expect(container).toContainText(formattedAmount);
  878  |     // Scheduled payment specific assertions
  879  |     if (timing === 'schedule') {
  880  |       await expect(container).toContainText('Scheduled for');
  881  |       if (scheduleDate) {
  882  |         // Convert scheduleDate format based on country
  883  |         let expectedDateFormat = scheduleDate;
  884  | 
  885  |         // If country is US, convert from DD/MM/YYYY to MM/DD/YYYY
  886  |         if (country === 'us') {
  887  |           const dateMatch = scheduleDate.match(/(\d{2})\/(\d{2})\/(\d{4})/);
  888  |           if (dateMatch) {
  889  |             const [, day, month, year] = dateMatch;
  890  |             expectedDateFormat = `${month}/${day}/${year}`;
  891  |             console.log(`🔄 Converted schedule date for US: ${scheduleDate} → ${expectedDateFormat}`);
  892  |           }
  893  |         }
  894  | 
  895  |         await expect(container).toContainText(expectedDateFormat);
  896  |         console.log(`✅ Verified scheduled date (${country.toUpperCase()}): ${expectedDateFormat}`);
  897  |       } else {
  898  |         console.warn('⚠️ scheduleDate not provided; skipping scheduled date text assertion.');
  899  |       }
  900  |     }
  901  | 
  902  |     // Membership + points verification
  903  |     // Skip "You will earn points" when paying with points-only (no points earned when redeeming points)
  904  |     // Partial points (points+card) will still show points earned on the card payment portion
  905  |     if (isPointsOnly) {
  906  |       // Points-only payment: no earned-points assertion in summary
  907  |       console.log('ℹ️ Points-only payment: Skipping "You will earn points" verification (no points earned when redeeming points)');
  908  |     } else {
  909  |       // For split points payments, derive expected points from the amount shown beside
  910  |       // "(Points earned on this amount)" because the UI rounds that value directly.
  911  |       if (isSplitPointsPayment) {
  912  |         const containerText = await container.textContent();
  913  |         const earnedAmountMatch = containerText.match(/\(Points earned on this amount\)[^\$]*\$?([\d,]+\.\d{2})/);
  914  | 
  915  |         if (earnedAmountMatch) {
  916  |           const earnedAmountFromUI = parseFloat(earnedAmountMatch[1].replace(/,/g, ''));
  917  |           points = Math.round(earnedAmountFromUI);
  918  |           points = this._applyTierPointsAdjustment(points, membershipTier);
  919  |           formattedPoints = points.toLocaleString();
  920  |           console.log(`🔍 Points recalculated from UI earned amount:`);
  921  |           console.log(`   Earned amount (from UI): $${earnedAmountFromUI.toFixed(2)}`);
  922  |           console.log(`   Points earned (final): ${points}`);
  923  |         } else {
  924  |           console.warn('⚠️ Could not extract earned amount from UI for split points payment, using initial calculation');
  925  |         }
  926  |       }
  927  | 
  928  |       // All other payment methods including split points flows: verify points earning
  929  |       await this._assertEarnedPointsText(container, formattedPoints, 'Summary', membershipTier);
  930  |     }
  931  | 
  932  |     // 2. Verify fees section (atomic)
  933  |     if (totalFees > 0) {
  934  |       await expect(container).toContainText('Fees');
  935  |       const containerTextForFees = (await container.textContent()) || '';
  936  |       if (usBankRenamedProgramFee) {
  937  |         // US bank: UI shows "Payment processing fee" (no percentage text, no "Total fees" line)
  938  |         await expect(container).toContainText('Payment processing fee');
  939  |         await expect(container).toContainText(formattedProgramFee);
  940  |         // US bank does NOT show "Total fees" line - skip it
  941  |       } else {
  942  |         // Program fee line (strict)
  943  |         if (programFee > 0) {
  944  |           if (feeText) {
  945  |             // Use regex to handle extra whitespace between "program fee" and percentage
  946  |             const feeTextPattern = new RegExp(`program fee\\s+${feeText.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')}`);
  947  |             await expect(container).toContainText(feeTextPattern);
  948  |           } else {
  949  |             await expect(container).toContainText('program fee');
  950  |           }
  951  |           await expect(container).toContainText(formattedProgramFee);
  952  |         } else {
  953  |           console.log('ℹ️ Program fee is $0.00; skipping program fee amount assertion.');
  954  |         }
  955  |       }
  956  |       if (processingFee > 0 && processingFeePercentageText) {
  957  |         // Check for processing fee percentage only (country-specific tax labels vary: AU="inc. GST", US="inc. Sales Tax")
  958  |         const processingFeePattern = new RegExp(`Processing fee\\s*\\*?\\s*\\([^)]*${processingFeePercentageText.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')}`, 'i');
  959  |         await expect(container).toContainText(processingFeePattern, { timeout: 10000 });
  960  | 
  961  |         // Direct string match - no tolerance
  962  |         await expect(container).toContainText(formattedProcessingFee);
  963  | 
  964  |         await expect(container).toContainText(/Processing fee (is charged|only applies)/i, { timeout: 10000 });
  965  |       }
  966  | 
  967  |       // Only verify "Total fees" for non-US scenarios
  968  |       // US payments don't show "Total fees" line - they only show "Fees" with individual fee breakdowns
  969  |       if (country !== 'us') {
  970  |         await expect(container).toContainText('Total fees');
  971  |         // Direct string match - no tolerance
  972  |         await expect(container).toContainText(formattedTotalFees);
  973  |       }
  974  |     }
  975  | 
  976  |     // 3. Verify total section (separate assertions)
> 977  |     await expect(container).toContainText(new RegExp(`Total\\s*${this._escapeRegex(formattedTotalAmount)}`));
       |                             ^ Error: expect(locator).toContainText(expected) failed
  978  | 
  979  |     // 4. Verify "Paying from" section based on payment method
  980  |     // Note: isPointsPlusCard already declared earlier with points calculation
  981  |     if (isPointsPlusCard) {
  982  |       await expect(this.paymentSummaryContainer).toContainText(/Paying from\s*Points\s*redeemed-\s*[\d,]+\s*PTS/i);
  983  |       // Enforce exact brand casing for supported brands (no tolerance)
  984  |       const allowedBrands = ['Mastercard', 'Visa', 'American Express'];
  985  |       if (!allowedBrands.includes(cardBrand)) {
  986  |         throw new Error(`Summary strict: unexpected card brand '${cardBrand}'. Allowed: ${allowedBrands.join(', ')}`);
  987  |       }
  988  |       // Flexible check: handle variable number of asterisks (UI may show * or ****)
  989  |       // BUG: https://eonx.atlassian.net/browse/SIR-4036
  990  |       // OLD IMPLEMENTATION: Look for brand + card number (e.g., "Visa *1000")
  991  |       // const brandPattern = new RegExp(`${this._escapeRegex(cardBrand)}\\s*\\*+\\s*${this._escapeRegex(cardLast4)}`);
  992  | 
  993  |       // NEW IMPLEMENTATION: Look for card number only with asterisk (e.g., "*1000")
  994  |       const brandPattern = new RegExp(`\\*+\\s*${this._escapeRegex(cardLast4)}`);
  995  |       await expect(this.paymentSummaryContainer).toContainText(brandPattern);
  996  | 
  997  |       // Verify the three detailed payment lines for partial points payment:
  998  |       // Line 1: Points redeemed amount should appear
  999  |       // Line 2: Card line with "(Points earned on this amount)" and base amount
  1000 |       await expect(this.paymentSummaryContainer).toContainText('(Points earned on this amount)');
  1001 |       await expect(this.paymentSummaryContainer).toContainText(formattedAmount);
  1002 | 
  1003 |       // Line 3: Card line with "(Total fees)" if fees > 0
  1004 |       if (totalFees > 0) {
  1005 |         await expect(this.paymentSummaryContainer).toContainText('(Total fees)');
  1006 |         await expect(this.paymentSummaryContainer).toContainText(formattedTotalFees);
  1007 |       }
  1008 |     } else if (normalizedPaymentMethod === 'payid') {
  1009 |       await expect(container).toContainText('Paying from');
  1010 |       await expect(container).toContainText('PayID (Amount)');
  1011 |       await expect(container).toContainText(formattedAmount);
  1012 | 
  1013 |       if (hasCardSelection) {
  1014 |         if (!cardLast4) {
  1015 |           throw new Error('Card details not available for PayID summary verification. Ensure cardLast4 is provided in payment options.');
  1016 |         }
  1017 |         const maskedCardPattern = new RegExp(`\\*+\\s*${this._escapeRegex(cardLast4)}`);
  1018 |         await expect(container).toContainText(maskedCardPattern);
  1019 |       } else if (hasBankSelection) {
  1020 |         if (bankAccountDisplayName) {
  1021 |           const compactBankDisplayName = bankAccountDisplayName.replace(/\s+/g, '').trim();
  1022 |           const compactContainerText = ((await container.textContent()) || '').replace(/\s+/g, '');
  1023 |           expect(compactContainerText).toContain(compactBankDisplayName);
  1024 |         }
  1025 |         const selectedBankAccountNo = paymentOptions.bankAccountNo ? String(paymentOptions.bankAccountNo) : '';
  1026 |         if (selectedBankAccountNo) {
  1027 |           const maskedBankPattern = new RegExp(`\\*+\\s*${this._escapeRegex(selectedBankAccountNo.slice(-4))}`);
  1028 |           await expect(container).toContainText(maskedBankPattern);
  1029 |         }
  1030 |       }
  1031 | 
  1032 |       if (totalFees > 0) {
  1033 |         await expect(container).toContainText('(Total fees)');
  1034 |         await expect(container).toContainText(formattedTotalFees);
  1035 |       }
  1036 |     } else if (['creditdebit', 'credit', 'debit', 'card'].includes(normalizedPaymentMethod)) {
  1037 |       if (!cardBrand || !cardLast4) {
  1038 |         throw new Error('Card details not available for verification. Ensure cardBrand and cardLast4 are provided in payment options.');
  1039 |       }
  1040 |       // Enforce exact brand casing for supported brands (no tolerance)
  1041 |       const allowedBrands = ['Mastercard', 'Visa', 'American Express'];
  1042 |       if (!allowedBrands.includes(cardBrand)) {
  1043 |         throw new Error(`Summary strict: unexpected card brand '${cardBrand}'. Allowed: ${allowedBrands.join(', ')}`);
  1044 |       }
  1045 |       // Card paying from (break into atomic checks)
  1046 |       await expect(container).toContainText('Paying from');
  1047 |       // Flexible check: handle variable number of asterisks (UI may show * or ****)
  1048 |       // BUG: https://eonx.atlassian.net/browse/SIR-4036
  1049 |       // OLD IMPLEMENTATION: Look for brand + card number (e.g., "Visa *1000")
  1050 |       // const payingFromBrand = new RegExp(`${this._escapeRegex(cardBrand)}\\s*\\*+\\s*${this._escapeRegex(cardLast4)}`);
  1051 | 
  1052 |       // NEW IMPLEMENTATION: Look for card number only with asterisk (e.g., "*1000")
  1053 |       const payingFromBrand = new RegExp(`\\*+\\s*${this._escapeRegex(cardLast4)}`);
  1054 |       await expect(container).toContainText(payingFromBrand);
  1055 |       await expect(container).toContainText(formattedAmount);
  1056 |       if (totalFees > 0) {
  1057 |         await expect(container).toContainText('(Total fees)');
  1058 | 
  1059 |         // Direct string match - no tolerance
  1060 |         await expect(container).toContainText(formattedTotalFees);
  1061 |       }
  1062 |     } else if (['bank', 'bankaccount', 'bank account'].includes(normalizedPaymentMethod)) {
  1063 |       // Extract the actual display text from the page to handle different formats
  1064 |       const pageText = await this.paymentSummaryContainer.textContent();
  1065 | 
  1066 |       // Try to match the "Paying from" section which could be:
  1067 |       // Format 1: "Bank Account 063-238, *6789" (generic bank account label)
  1068 |       // Format 2: "First Last ****6789" (account holder name)
  1069 |       const payingFromMatch = pageText.match(/Paying from\s*([^$]+?)\s*\$[\d,]+\.\d{2}/);
  1070 | 
  1071 |       let accountDisplayName = 'Account Holder';
  1072 |       if (payingFromMatch && payingFromMatch[1]) {
  1073 |         accountDisplayName = payingFromMatch[1].trim();
  1074 |         console.log('✅ Extracted account display from page:', accountDisplayName);
  1075 |       } else if (bankAccountDisplayName) {
  1076 |         const nameMatch = bankAccountDisplayName.match(/^([^0-9]+?)(?:\s+\d|$)/);
  1077 |         if (nameMatch && nameMatch[1]) {
```