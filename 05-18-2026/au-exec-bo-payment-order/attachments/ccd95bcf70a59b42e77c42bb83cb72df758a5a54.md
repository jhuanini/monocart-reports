# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests/ebr/core/payments/payment-order.spec.js >> business owner - bank account - payid - now
- Location: utils/ebr/runtime-skip.js:399:27

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('#sidebar-content')
Timeout: 5000ms
- Expected substring  -  1
+ Received string     + 14

- Fees
+ Arrow back
+ BackView Tax Invoice
+   
+
+ Payment successful Transaction ID #PXE5WK Status: Completed Date: 18 May 2026, 11:44 am
+   
+
+ Payment initiated18 May 2026,11:44 am
+   
+
+ Funds received18 May 2026,11:45 am
+   
+
+ Funds disbursed18 May 2026,11:45 amPaying on behalf ofTest BusinessPaying toDon't Delete PayeeBSB: 063-238Account number: 123456789Amount$1,527.30 Reference appears on payee statement: c80il6tpTotal$1,527.30Paying fromPayID$1,527.30

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('#sidebar-content')
    14 × locator resolved to <div id="sidebar-content" class="relative h-full w-full">…</div>
       - unexpected value "Arrow back
BackView Tax Invoice
  

Payment successful Transaction ID #PXE5WK Status: Completed Date: 18 May 2026, 11:44 am
  

Payment initiated18 May 2026,11:44 am
  

Funds received18 May 2026,11:45 am
  

Funds disbursed18 May 2026,11:45 amPaying on behalf ofTest BusinessPaying toDon't Delete PayeeBSB: 063-238Account number: 123456789Amount$1,527.30 Reference appears on payee statement: c80il6tpTotal$1,527.30Paying fromPayID$1,527.30"

```

```yaml
- button "Back"
- button "View Tax Invoice"
- text: "Payment successful Transaction ID #PXE5WK Status: Completed Date: 18 May 2026, 11:44 am Payment initiated 18 May 2026, 11:44 am Funds received 18 May 2026, 11:45 am Funds disbursed 18 May 2026, 11:45 am Paying on behalf of Test Business Paying to Don't Delete Payee BSB: 063-238 Account number: 123456789 Amount $1,527.30 Reference appears on payee statement: c80il6tp Total $1,527.30 Paying from PayID $1,527.30"
```

# Test source

```ts
  314 | 
  315 |     const expectedDate = this.formatCreatedAtForActivity(context.createdAt, context.country);
  316 |     await expect(container).toContainText(new RegExp(`Date:\\s*${this.escapeRegex(expectedDate)}`));
  317 |     console.log(`✅ Activity date verified: ${expectedDate}`);
  318 |   }
  319 | 
  320 |   /**
  321 |    * Verify timeline milestone labels. These are non-fatal on confirmation, so
  322 |    * they remain non-fatal here too.
  323 |    *
  324 |    * @param {import('@playwright/test').Locator} container
  325 |    * @param {Object} context
  326 |    */
  327 |   async verifyMilestones(container, context) {
  328 |     if (context.isScheduled) {
  329 |       console.log('ℹ️ Skipping activity milestone assertions for scheduled payment');
  330 |       return;
  331 |     }
  332 | 
  333 |     for (const milestone of ['Payment initiated', 'Funds received', 'Funds disbursed']) {
  334 |       try {
  335 |         await expect(container).toContainText(milestone);
  336 |       } catch (error) {
  337 |         console.warn(`Milestone '${milestone}' not found in activity detail (non-fatal): ${error.message}`);
  338 |       }
  339 |     }
  340 |   }
  341 | 
  342 |   /**
  343 |    * Verify the selected business / paying on behalf section.
  344 |    *
  345 |    * @param {import('@playwright/test').Locator} container
  346 |    * @param {Object} context
  347 |    */
  348 |   async verifyPayingOnBehalf(container, context) {
  349 |     const selectedBusiness = context.summaryData.selectedBusiness;
  350 |     if (!selectedBusiness) {
  351 |       console.warn('⚠️ Selected business not available in summary data');
  352 |       return;
  353 |     }
  354 | 
  355 |     // BUG: Missing ABN in paying on behalf of
  356 |     // await expect(container).toContainText('Paying on behalf of');
  357 |     // await expect(container).toContainText(selectedBusiness);
  358 |     // console.log(`✅ Activity paying on behalf verified: ${selectedBusiness}`);
  359 |   }
  360 | 
  361 |   /**
  362 |    * Verify the Paying to section.
  363 |    *
  364 |    * @param {import('@playwright/test').Locator} container
  365 |    * @param {Object} context
  366 |    */
  367 |   async verifyPayingTo(container, context) {
  368 |     const { summaryData, payeeDetails, country, isSponsoredBiller, isBatchOrPayrollPayment } = context;
  369 | 
  370 |     if (isBatchOrPayrollPayment) {
  371 |       await expect(container).toContainText(`Batch file: ${summaryData.batchFileName || 'batch file'}`);
  372 |       console.log('ℹ️ Activity batch/payroll paying-to check used batch file name');
  373 |       return;
  374 |     }
  375 | 
  376 |     await expect(container).toContainText('Paying to');
  377 |     if (payeeDetails.name) {
  378 |       await expect(container).toContainText(payeeDetails.name);
  379 |     }
  380 | 
  381 |     if (!isSponsoredBiller && summaryData.displayBsb) {
  382 |       if (String(payeeDetails.type || '').toLowerCase().includes('bpay')) {
  383 |         await expect(container).toContainText(new RegExp(`Biller\\s*Code:\\s*${this.escapeRegex(summaryData.displayBsb)}`, 'i'));
  384 |       } else {
  385 |         await expect(container).toContainText(new RegExp(`${this.escapeRegex(country === 'us' ? 'Routing Number' : 'BSB')}:\\s*${this.escapeRegex(summaryData.displayBsb)}`, 'i'));
  386 |       }
  387 |     }
  388 | 
  389 |     if (summaryData.displayAccountNo) {
  390 |       if (String(payeeDetails.type || '').toLowerCase().includes('bpay')) {
  391 |         await expect(container).toContainText(new RegExp(`Reference:\\s*${this.escapeRegex(summaryData.displayAccountNo)}`, 'i'));
  392 |       } else {
  393 |         await expect(container).toContainText(new RegExp(`Account\\s*number:\\s*${this.escapeRegex(summaryData.displayAccountNo)}`, 'i'));
  394 |       }
  395 |     }
  396 | 
  397 |     if (summaryData.reference && !String(payeeDetails.type || '').toLowerCase().includes('bpay') && !isSponsoredBiller) {
  398 |       await expect(container).toContainText(new RegExp(`Reference appears on payee statement:\\s*${this.escapeRegex(summaryData.reference)}`, 'i'));
  399 |     }
  400 |   }
  401 | 
  402 |   /**
  403 |    * Verify amount, fees, and total.
  404 |    *
  405 |    * @param {import('@playwright/test').Locator} container
  406 |    * @param {Object} context
  407 |    */
  408 |   async verifyAmountAndFees(container, context) {
  409 |     const { summaryData, country, hasCardFundingSource, normalizedPaymentMethod } = context;
  410 | 
  411 |     await expect(container).toContainText(new RegExp(`Amount\\s*${this.escapeRegex(summaryData.formattedAmount)}`));
  412 | 
  413 |     if (normalizedPaymentMethod === 'payid') {
> 414 |       await expect(container).toContainText('Fees');
      |                               ^ Error: expect(locator).toContainText(expected) failed
  415 |       console.log('ℹ️ PayID main activity: fee breakdown is verified on the linked fee-payment activity');
  416 |       return;
  417 |     }
  418 | 
  419 |     if (summaryData.formattedTotalFees && summaryData.formattedTotalFees !== '$0.00') {
  420 |       if (country === 'au') {
  421 |         if (summaryData.feeText) {
  422 |           await expect(container).toContainText(`program fee ${summaryData.feeText}`);
  423 |         } else {
  424 |           await expect(container).toContainText('program fee');
  425 |         }
  426 | 
  427 |         if (summaryData.formattedProgramFee && summaryData.formattedProgramFee !== '$0.00') {
  428 |           await expect(container).toContainText(summaryData.formattedProgramFee);
  429 |         }
  430 | 
  431 |         const shouldVerifyProcessingFee =
  432 |           summaryData.formattedProcessingFee !== '$0.00' && (['creditdebit', 'credit', 'debit', 'card', 'pba', 'mastercard'].includes(summaryData.paymentMethod) || (summaryData.paymentMethod === 'points' && hasCardFundingSource));
  433 | 
  434 |         if (shouldVerifyProcessingFee) {
  435 |           if (summaryData.processingFeePercentageText) {
  436 |             await expect(container).toContainText(new RegExp(`processing fee\\*?.*${this.escapeRegex(summaryData.processingFeePercentageText)}`, 'i'));
  437 |           }
  438 |           await expect(container).toContainText(/processing fee\*?.*\$\d+\.\d{2}/i);
  439 |         }
  440 | 
  441 |         await expect(container).toContainText(new RegExp(`Total fees\\s*${this.escapeRegex(summaryData.formattedTotalFees)}`));
  442 |       } else {
  443 |         await expect(container).toContainText('Payment processing fee');
  444 |         if (summaryData.formattedProgramFee && summaryData.formattedProgramFee !== '$0.00') {
  445 |           await expect(container).toContainText(summaryData.formattedProgramFee);
  446 |         }
  447 |       }
  448 |     }
  449 | 
  450 |     await expect(container).toContainText(new RegExp(`Total\\s*${this.escapeRegex(summaryData.formattedTotalAmount)}`));
  451 |   }
  452 | 
  453 |   /**
  454 |    * Detect the linked PayID fee-payment activity. It references the original
  455 |    * PayID transaction but has its own fee transaction header.
  456 |    *
  457 |    * @param {import('@playwright/test').Locator} container
  458 |    * @param {Object} context
  459 |    * @returns {Promise<boolean>}
  460 |    */
  461 |   async isPayIdFeePaymentActivity(container, context) {
  462 |     if (context.normalizedPaymentMethod !== 'payid') {
  463 |       return false;
  464 |     }
  465 | 
  466 |     const text = await container.textContent().catch(() => '');
  467 |     return /Fee payment/i.test(text || '') && /Fees for payment/i.test(text || '');
  468 |   }
  469 | 
  470 |   /**
  471 |    * Verify the PayID fee-payment detail view using values captured from the
  472 |    * summary/confirmation flow.
  473 |    *
  474 |    * @param {string} transactionId
  475 |    * @param {import('@playwright/test').Locator} container
  476 |    * @param {Object} context
  477 |    */
  478 |   async verifyPayIdFeePaymentActivityDetails(transactionId, container, context) {
  479 |     const { summaryData, country, hasCardFundingSource } = context;
  480 | 
  481 |     await expect(container).toContainText(/Fee payment pending/i);
  482 |     await expect(container).toContainText(/Status:\s*Pending/i);
  483 |     await expect(container).toContainText('Fees for payment');
  484 |     await expect(container.getByText(`Transaction ID #${transactionId}`)).toBeVisible({ timeout: 30000 });
  485 |     await expect(container).toContainText('Fees');
  486 | 
  487 |     if (country === 'au') {
  488 |       if (summaryData.feeText) {
  489 |         await expect(container).toContainText(new RegExp(`program fee\\s*${this.escapeRegex(summaryData.feeText)}`, 'i'));
  490 |       } else {
  491 |         await expect(container).toContainText(/program fee/i);
  492 |       }
  493 | 
  494 |       if (summaryData.formattedProgramFee && summaryData.formattedProgramFee !== '$0.00') {
  495 |         await expect(container).toContainText(summaryData.formattedProgramFee);
  496 |       }
  497 | 
  498 |       // BUG: Processing fee mismatch from summary
  499 |       // const shouldVerifyProcessingFee = summaryData.formattedProcessingFee !== '$0.00' && hasCardFundingSource;
  500 |       // if (shouldVerifyProcessingFee) {
  501 |       //   if (summaryData.processingFeePercentageText) {
  502 |       //     await expect(container).toContainText(new RegExp(`processing fee\\s*\\([^)]*${this.escapeRegex(summaryData.processingFeePercentageText)}`, 'i'));
  503 |       //   } else {
  504 |       //     await expect(container).toContainText(/processing fee/i);
  505 |       //   }
  506 |       //   await expect(container).toContainText(summaryData.formattedProcessingFee);
  507 |       // }
  508 | 
  509 |       // BUG: Processing fee mismatch
  510 |       // await expect(container).toContainText(new RegExp(`Total fees\\s*${this.escapeRegex(summaryData.formattedTotalFees)}`, 'i'));
  511 |     } else {
  512 |       await expect(container).toContainText('Payment processing fee');
  513 |       if (summaryData.formattedProgramFee && summaryData.formattedProgramFee !== '$0.00') {
  514 |         await expect(container).toContainText(summaryData.formattedProgramFee);
```