# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests/ebr/core/payments/payment-order.spec.js >> authorised maker - bank account - bank - schedule
- Location: utils/ebr/runtime-skip.js:320:14

# Error details

```
Error: Payee 'Don't delete payee' not found in selector list.
```

# Page snapshot

```yaml
- generic [ref=e3]:
  - banner [ref=e4]:
    - generic [ref=e5]:
      - link "Go to Home" [ref=e7] [cursor=pointer]:
        - /url: /
        - img "Symbion Elite Rewards Logo" [ref=e9]
      - generic [ref=e10]:
        - generic [ref=e11]:
          - button "PLATINUM" [ref=e12] [cursor=pointer]:
            - button "PLATINUM" [ref=e13]:
              - generic [ref=e15]:
                - img [ref=e17]
                - paragraph [ref=e36]: PLATINUM
          - button "Action" [ref=e38] [cursor=pointer]
        - list [ref=e41]:
          - listitem [ref=e42]:
            - button [ref=e43] [cursor=pointer]:
              - img [ref=e45]
          - listitem [ref=e48]:
            - button [ref=e49] [cursor=pointer]:
              - img [ref=e51]
          - listitem [ref=e53]:
            - button [ref=e54] [cursor=pointer]:
              - img [ref=e56]
  - main [ref=e59]:
    - generic [ref=e61]:
      - navigation "Benefits" [ref=e64]:
        - link "Home" [ref=e65] [cursor=pointer]:
          - /url: /
          - generic [ref=e66]:
            - img
          - generic [ref=e67]: Home
        - link "Make a payment" [ref=e68] [cursor=pointer]:
          - /url: /payments/make
          - generic [ref=e69]:
            - img
          - generic [ref=e70]: Make a payment
        - button "Payment methods" [ref=e71] [cursor=pointer]:
          - img [ref=e73]
          - generic [ref=e75]: Payment methods
        - link "Statement accounts" [ref=e76] [cursor=pointer]:
          - /url: /statements
          - generic [ref=e77]:
            - img
          - generic [ref=e78]: Statement accounts
        - link "Payees" [ref=e79] [cursor=pointer]:
          - /url: /payments/payees
          - generic [ref=e80]:
            - img
          - generic [ref=e81]: Payees
        - link "Scheduled payments" [ref=e82] [cursor=pointer]:
          - /url: /scheduled-payments
          - generic [ref=e83]:
            - img
          - generic [ref=e84]: Scheduled payments
        - link "Batch payment" [ref=e85] [cursor=pointer]:
          - /url: /bank-file-upload
          - generic [ref=e86]:
            - img
          - generic [ref=e87]: Batch payment
        - link "Rewards" [ref=e89] [cursor=pointer]:
          - /url: /gift-cards
          - generic [ref=e90]:
            - img
          - generic [ref=e91]: Rewards
        - link "Experiences" [ref=e92] [cursor=pointer]:
          - /url: /competitions
          - generic [ref=e93]:
            - img
          - generic [ref=e94]: Experiences
        - link "BizHub" [ref=e95] [cursor=pointer]:
          - /url: /community-hub
          - generic [ref=e96]:
            - img
          - generic [ref=e97]: BizHub
        - link "Transfer my points" [ref=e99] [cursor=pointer]:
          - /url: /points-programs
          - generic [ref=e100]:
            - img
          - generic [ref=e101]: Transfer my points
        - link "Activity" [ref=e103] [cursor=pointer]:
          - /url: /activity
          - generic [ref=e104]:
            - img
          - generic [ref=e105]: Activity
      - list [ref=e112]:
        - listitem [ref=e113]:
          - link "How it works" [ref=e114] [cursor=pointer]:
            - /url: https://support.symbionelite.com.au/hc/en-us
            - generic [ref=e115]: How it works
        - listitem [ref=e116]:
          - link "Terms & Conditions" [ref=e117] [cursor=pointer]:
            - /url: /terms-conditions
            - generic [ref=e118]: Terms & Conditions
        - listitem [ref=e119]:
          - link "Privacy Policy" [ref=e120] [cursor=pointer]:
            - /url: /privacy-policy
            - generic [ref=e121]: Privacy Policy
        - listitem [ref=e122]:
          - link "Contact" [ref=e123] [cursor=pointer]:
            - /url: /help
            - generic [ref=e124]: Contact
        - listitem [ref=e125]:
          - button "Action" [ref=e126] [cursor=pointer]: Logout
```

# Test source

```ts
  1001 |       return;
  1002 |     }
  1003 | 
  1004 |     // For creditdebit and bank, schedule payment should be enabled
  1005 |     if (['creditdebit', 'credit', 'debit', 'card', 'bank', 'banktransfer'].includes(method)) {
  1006 |       await expect(this.selectWhenToPayContainer).toMatchAriaSnapshot(`
  1007 |       - radio "Pay now" [checked]
  1008 |       - text: Pay now
  1009 |       - radio "Schedule payment"
  1010 |       - text: Schedule payment
  1011 |     `);
  1012 |     } else {
  1013 |       await expect(this.selectWhenToPayContainer).toMatchAriaSnapshot(`
  1014 |       - radio "Pay now" [checked]
  1015 |       - text: Pay now
  1016 |       - radio "Schedule payment" [disabled]
  1017 |       - text: Schedule payment
  1018 |     `);
  1019 |     }
  1020 |   }
  1021 | 
  1022 |   // ============================================================================
  1023 |   // ATOMIC METHODS
  1024 |   // ============================================================================
  1025 | 
  1026 |   /**
  1027 |    * Navigate to the Make Payment page
  1028 |    * Retries navigation if the hidden-grouped endpoint returns a non-200 (e.g. 504)
  1029 |    * due to concurrent workers hitting the backend simultaneously
  1030 |    */
  1031 |   async navigateToMakePayment() {
  1032 |     const maxAttempts = 3;
  1033 | 
  1034 |     for (let attempt = 1; attempt <= maxAttempts; attempt++) {
  1035 |       console.log(`🔄 navigateToMakePayment - Starting attempt ${attempt}/${maxAttempts}`);
  1036 |       await mocksForMakePayment(this.page);
  1037 |       await this.page.waitForLoadState('load');
  1038 | 
  1039 |       await this.makePaymentLink.click({ timeout: 60000 });
  1040 | 
  1041 |       try {
  1042 |         await Promise.all([
  1043 |           this.page.waitForResponse((r) => r.url().includes('/v3/points/balance') && r.status() === 200, { timeout: 60000 }),
  1044 |           // this.page.waitForResponse((r) => r.url().includes('/payees/hidden-grouped?perPage=1000&page=0') && r.request().method() === 'GET' && r.status() === 200, { timeout: 30000 }),
  1045 |         ]);
  1046 |         await expect(this.payeeSelector).toBeVisible({ timeout: 60000 });
  1047 |         return; // Success — exit the retry loop
  1048 |       } catch (error) {
  1049 |         if (attempt < maxAttempts) {
  1050 |           console.log(`⚠️ navigateToMakePayment attempt ${attempt}/${maxAttempts} failed (likely 504 from concurrent load), retrying...`);
  1051 |           await this.page.reload();
  1052 |         } else {
  1053 |           throw new Error(`❌ navigateToMakePayment failed after ${maxAttempts} attempts: ${error.message}`);
  1054 |         }
  1055 |       }
  1056 |     }
  1057 |   }
  1058 | 
  1059 |   /**
  1060 |    * Select the business (paying for) from the dropdown
  1061 |    * Used when multiple businesses are available
  1062 |    */
  1063 |   async selectPayingFor() {
  1064 |     await this.page
  1065 |       .waitForResponse((res) => res.url().includes('/user-businesses?orderBy') && res.request().method() === 'GET' && res.status() === 200, { timeout: 2000 })
  1066 |       .catch(() => {
  1067 |         console.log('ℹ️ /user-businesses 200 response not observed before selecting business, continuing...');
  1068 |       });
  1069 | 
  1070 |     await this.page.getByPlaceholder('Select business').click();
  1071 | 
  1072 |     // Wait for options to appear, retry click if not visible
  1073 |     const option = this.page.getByRole('option').nth(0);
  1074 |     try {
  1075 |       await option.waitFor({ state: 'visible', timeout: 2000 });
  1076 |     } catch (e) {
  1077 |       console.log('⚠️ Options not visible after first click, retrying...');
  1078 |       await this.page.getByPlaceholder('Select business').click();
  1079 |       await option.waitFor({ state: 'visible', timeout: 5000 });
  1080 |     }
  1081 | 
  1082 |     await option.click();
  1083 | 
  1084 |     // Save the selected business text for later use
  1085 |     this.selectedBusiness = await this.page.getByPlaceholder('Select business').textContent();
  1086 |     console.log(`✅ Selected business: ${this.selectedBusiness}`);
  1087 |   }
  1088 | 
  1089 |   /**
  1090 |    * Select a payee by searching and clicking the first result
  1091 |    * Automatically extracts and stores payee details (BSB, Account number, and type)
  1092 |    * Waits up to 5 seconds for the payee to appear, throws error if not found
  1093 |    * @param {string} payeeName - The payee name to search for
  1094 |    * @throws {Error} If the payee is not found within 5 seconds
  1095 |    */
  1096 |   async selectPayee(payeeName, testInfo) {
  1097 |     await this.payeeSelector.fill(payeeName);
  1098 |     await this.page.waitForTimeout(250); // brief debounce for dropdown render
  1099 |     const candidateItems = this.page.locator('div > ul > li > div').filter({ hasText: payeeName });
  1100 |     if ((await candidateItems.count().catch(() => 0)) === 0) {
> 1101 |       throw new Error(`Payee '${payeeName}' not found in selector list.`);
       |             ^ Error: Payee 'Don't delete payee' not found in selector list.
  1102 |     }
  1103 | 
  1104 |     try {
  1105 |       // Get the payee item with full details and wait for it to be visible
  1106 |       const payeeItem = this.page
  1107 |         .locator('div > ul > li > div', {
  1108 |           hasText: payeeName,
  1109 |         })
  1110 |         .first();
  1111 | 
  1112 |       // Wait for the payee item to appear with a 5-second timeout
  1113 |       await payeeItem.waitFor({ state: 'visible', timeout: 5000 });
  1114 | 
  1115 |       // Extract full text before clicking
  1116 |       const fullPayeeText = await payeeItem.innerText(); // log removed
  1117 | 
  1118 |       // Click to select the payee
  1119 |       await payeeItem.click();
  1120 | 
  1121 |       // Extract and parse payee details
  1122 |       await this.extractPayeeDetails(fullPayeeText);
  1123 |     } catch (error) {
  1124 |       // If the payee is not found within 5 seconds, throw a descriptive error
  1125 |       throw new Error(`Payee '${payeeName}' not found after searching. Please check if the payee name is correct and exists in the system.`);
  1126 |     }
  1127 |   }
  1128 | 
  1129 |   /**
  1130 |    * Extract payee details from the payee text
  1131 |    * Stores the payee name, BSB, account number, and type for later verification
  1132 |    * @param {string} payeeText - The full payee text from the selection list
  1133 |    */
  1134 |   async extractPayeeDetails(payeeText) {
  1135 |     try {
  1136 |       // Parse the payee text
  1137 |       // Examples:
  1138 |       // AU: "Don't delete payee- BSB/ACC: 063-238, 324473704\nBank account"
  1139 |       // US: "Don't delete payee- Routing No: 122-199-983, ACC: 364577914\nBank account"
  1140 |       // BPAY: "nabtrade... BPAY"
  1141 |       // Sponsored biller: "Sponsored biller name\nSponsored biller"
  1142 | 
  1143 |       const lines = payeeText.split('\n').filter((line) => line.trim() !== '');
  1144 | 
  1145 |       if (lines.length >= 1) {
  1146 |         // Extract payee type from the last line
  1147 |         const lastLine = lines[lines.length - 1].trim();
  1148 |         this.selectedPayeeDetails.type = lastLine;
  1149 | 
  1150 |         // Extract payee name and BSB/Account from the first line
  1151 |         const firstLine = lines[0].trim();
  1152 | 
  1153 |         const bankDetailsMatch = firstLine.match(/^(.*?)\s*-?\s*(?:BSB\/ACC|Routing\s*(?:No|Number))\s*:\s*(.+)$/i);
  1154 |         if (bankDetailsMatch) {
  1155 |           // Handle both AU and US formats
  1156 |           // AU Format: "Payee Name- BSB/ACC: 063-238, 324473704"
  1157 |           // US Format: "Payee Name- Routing No: 122-199-983, ACC: *2511" (masked in US)
  1158 |           // Also supports "Routing Number:"
  1159 |           this.selectedPayeeDetails.name = bankDetailsMatch[1].trim().replace(/-$/, '');
  1160 |           const bsbAccPart = bankDetailsMatch[2].trim();
  1161 | 
  1162 |           let bsb = '';
  1163 |           let accountNo = '';
  1164 | 
  1165 |           // Prefer explicit "ACC:" extraction when present.
  1166 |           const accountWithLabelMatch = bsbAccPart.match(/^(.*?)(?:,\s*|\s+)ACC:\s*([A-Za-z0-9*]+)\s*$/i);
  1167 |           if (accountWithLabelMatch) {
  1168 |             bsb = accountWithLabelMatch[1].trim().replace(/,$/, '');
  1169 |             accountNo = accountWithLabelMatch[2].trim();
  1170 |           } else {
  1171 |             // Fallback to comma-separated parse.
  1172 |             const [first, second] = bsbAccPart.split(',').map((part) => part.trim());
  1173 |             bsb = first || '';
  1174 |             accountNo = (second || '').replace(/^ACC:\s*/i, '').trim();
  1175 |           }
  1176 | 
  1177 |           this.selectedPayeeDetails.bsb = bsb;
  1178 | 
  1179 |           // For US, the dropdown shows masked account numbers (e.g., "*2511")
  1180 |           // Get the full account number from the API cache
  1181 |           if (accountNo && accountNo.startsWith('*')) {
  1182 |             // Wait for API data to be captured (up to 5 seconds)
  1183 |             const maxWait = 5000;
  1184 |             const startTime = Date.now();
  1185 |             while (!this.cachedPayeesData && Date.now() - startTime < maxWait) {
  1186 |               await this.page.waitForTimeout(100);
  1187 |             }
  1188 | 
  1189 |             const fullAccountNo = this.getFullAccountNumber(this.selectedPayeeDetails.name, accountNo);
  1190 |             if (fullAccountNo) {
  1191 |               this.selectedPayeeDetails.accountNo = fullAccountNo;
  1192 |               console.log(`✅ Using full account number from API: ${fullAccountNo} (was ${accountNo})`);
  1193 |             } else {
  1194 |               // Fallback to masked number if API data not available
  1195 |               this.selectedPayeeDetails.accountNo = accountNo;
  1196 |               console.warn(`⚠️ Using masked account number (API data not available): ${accountNo}`);
  1197 |             }
  1198 |           } else {
  1199 |             // AU or full account number already shown
  1200 |             this.selectedPayeeDetails.accountNo = accountNo;
  1201 |           }
```